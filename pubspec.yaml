name: orange_planet_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  material_design_icons_flutter: ^4.0.5045
  flutter_launcher_icons: ^0.7.5
  google_fonts: ^1.0.0

  cupertino_icons: ^0.1.2

flutter_icons:
  image_path: "icon/icon.png"
  android: true
  ios: true

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - icon/logoPrince.png
    - icon/icon.png

