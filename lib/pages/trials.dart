import 'package:flutter/material.dart';

class Trials extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar (
        title: Text('Испытания'),
      ),
      body: Center(
        child: Text('This is Trials page'),
      ),
    );
  }
}